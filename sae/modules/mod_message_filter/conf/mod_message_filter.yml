modules:
  mod_message_filter:
    api_url: "${MESSAGE_FILTER_API_URL}"
    api_timeout: 5000
    api_key: "${MESSAGE_FILTER_API_KEY}"
    filter_groupchat: false
    filter_types:
      - chat
      - normal
